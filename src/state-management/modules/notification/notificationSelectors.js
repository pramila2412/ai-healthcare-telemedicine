export const selectNotification = (state) => state.notification;

export const selectIsNotificationOpen = (state) => state.notification.isOpen;

export const selectNotificationMessage = (state) => state.notification.message;

export const selectNotificationIcon = (state) => state.notification.icon;
